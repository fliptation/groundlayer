export const metadata = {
  title: "Ideas — Ground Layer",
  description:
    "Share and vote on ideas for building sustainable community infrastructure.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
