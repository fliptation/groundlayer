export const metadata = {
  title: "Discussions — Ground Layer",
  description:
    "Join community discussions about sustainable infrastructure across all layers of society.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
