export const metadata = {
  title: "Examples — Ground Layer",
  description:
    "Discover real-world examples of sustainable infrastructure projects across all layers.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
