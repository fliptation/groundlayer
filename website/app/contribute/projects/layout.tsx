export const metadata = {
  title: "Projects — Ground Layer",
  description:
    "Explore and submit real-world projects building sustainable community infrastructure.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
